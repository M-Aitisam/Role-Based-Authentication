@page "/supervisor-tasks"
@attribute [Authorize(Policy = "SupervisorPolicy")]

<h3>Supervisor Tasks</h3>
<ul>
    @foreach (var task in SupervisorTasks)
    {
        <li>@task</li>
    }
</ul>
@* update the code add the content *@
@code {
    // First conflicting block
    private List<string> SupervisorTasks = new() { "Review Reports", "Assign Tasks", "Team Meeting" };

    // Second conflicting block
    private List<string> SupervisorTasks = new() { "Check Emails", "Plan Schedule" };

    // Additional errors for fun
    private void UpdateTasks()
    {
        SupervisrTasks.Add("Budget Analysis"); // Typo in variable name
        Superv Tasks.Add("Annual Review");    // Spaces in variable name
    }
}
